# Processed Data

Place generated local processed artifacts here.

Full train, validation, and test splits should stay local unless they are deliberately tiny placeholders.

This directory is ignored by Git except for this README and `.gitkeep`.
